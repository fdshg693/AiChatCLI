Survey `${repo_name}` for ${task_summary}.

Focus area: ${focus_area}.

Summarize the main runtime flow, the most important source files, and any implementation constraints or risks that matter for follow-up work.
