Implement the following feature in `${repo_name}`: ${task_summary}.

Acceptance criteria: ${acceptance_criteria}.

Make the necessary code changes, keep the existing architecture coherent, and update docs or configuration when needed.
