Review the implementation for `${repo_name}` against the requested task.

Focus on correctness, regressions, missing edge cases, architectural fit, and whether the acceptance criteria were actually met.
